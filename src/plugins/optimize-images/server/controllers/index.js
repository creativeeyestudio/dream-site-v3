'use strict';

const myController = require('./my-controller');
const upload = require('./upload');
const settings = require('./settings');

module.exports = {
  myController,
  upload,
  settings
};
